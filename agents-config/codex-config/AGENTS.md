## Deletion Safety

- **NEVER use `rm -rf`** - destructive and unrecoverable
- Use `trash` instead: `trash folder-name` or `trash file.txt`
  - Works like `rm -rf` but moves to Trash so files can be recovered
